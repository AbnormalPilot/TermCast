import AppKit

// Entry point wired in P1-T15 (TermCastApp.swift full implementation).
// @main removed here to avoid conflict with top-level code in other targets
// that SourceKit may index into this module.
final class AppDelegate: NSObject, NSApplicationDelegate {
    func applicationDidFinishLaunching(_ notification: Notification) {}
}
